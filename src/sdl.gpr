project SDL is
    SDL2_LIB_DIR := external ("SDL2_LIB_DIR", "/usr/lib");
    for Externally_Built use "true";
    for Library_Dir use SDL2_LIB_DIR;
    for Library_Name use "SDL2";
    for Source_Dirs use ();
    for Library_Kind use "dynamic";
end SDL;
